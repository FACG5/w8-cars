<section class="main__container--limited-width ">
  <h1 class="car_brand">{{info.title}}</h1>
  <img class="hero" src="{{info.img_car}}" alt="{{info.title}}" />
  <p>{{info.description}}</p>
</section>
